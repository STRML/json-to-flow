// @flow
<% if (modelSuperClass) { -%>
import {<%= modelSuperClass %>} from '<%= modelSuperClassPath %>';
<% } -%>
<% -%>
<% Object.keys(additionalTypes).forEach(function(value, key) { -%>
export type <%= key %> = <%- value %>; <% /* TODO */ %>
<% }); -%>

export class <%= modelName %>Model <%= modelSuperClass ? 'extends ' + modelSuperClass : '' %> {
<% Object.keys(modelSchema).forEach(function(key) { -%>
  <%= key %>: <%- modelSchema[key].type %>;
<% }); -%>
}
